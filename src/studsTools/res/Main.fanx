using concurrent
using studs

**
** Entry point for studs application.
**
const class Main
{
  static Int main()
  {
    // Obligatory Hello, world
    echo("Hello, world from Studs :)")

    // Sleep forever to keep Fantom running
    Actor.sleep(Duration.maxVal)
    return 0
  }
}